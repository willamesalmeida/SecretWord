import "./StartScreen.css";

const StartScreen = ({StartScreen}) => {
  return (
    <div className="start">
        <h1>Secret World</h1>
        <p>Clique no botão abaixo para começar a jogar </p>
        <button onClick={}>Começar o jogo</button>
    </div>
  )
};

export default StartScreen